[project]
name = "mock-store"
version = "0.1.0"
description = "Local HTTP target for smoke-testing and Phase 1's MockStoreAdapter -- not a real marketplace."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<0.116",
    "uvicorn[standard]>=0.32,<0.33",
]

[tool.uv]
package = false
